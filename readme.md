#Simple app for Digital Ocean Docker Auto Deploy tutorial

This is a very simple app that uses curl to retreive weather at a specific airport by inputting the location's airport code.

For a list of airport codes, go to (airportcodes)[www.airportcodes.org/].

To view the entire Docker auto deploy tutorial on Digital Ocean's blog, (go here TBD)[]: